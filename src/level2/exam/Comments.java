package level2.exam;

import java.util.Arrays;
import java.util.Random;

public class Comments {
    public static void main(String[] args) {
        /*
        Test - Arrays.sort();
        Separate class for each task.
        First two tasks without any args(1..5), any arrays & <=.
        Formatted code & remove empty lines, necessary extra lines,
        Names(a, b, n),
        Too complicated algorithms,
        return result instead of sout.

        Task 2 return double
        Task 3 better to use ternary operator
        Task 4 Random.nextBoolean
        */
    }
}
